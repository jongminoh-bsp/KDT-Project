name: 📢 Slack Notification (DISABLED)

# 중복 알림 방지를 위해 비활성화
# on:
#   push:
#     branches: [ main, develop, staging ]
#   pull_request:
#     branches: [ main, develop, staging ]
#   workflow_run:
#     workflows: ["🤖 Q Auto Analysis & Deploy", "🏗️ Terraform Deploy", "🔍 Unused Resource Scanner"]
#     types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
    - name: 📢 Send Slack Notification
      run: echo "이 워크플로우는 중복 알림 방지를 위해 비활성화되었습니다."
